# 14_rerank_hybrid

本章不是孤立 demo，而是 `enterprise_rag_memory` 的第 14 章阶段入口。

连续项目：企业知识库 RAG + 长期记忆连续项目。

运行本章：

```bash
python3 14_rerank_hybrid/demo.py
```

运行同一阶段的统一入口：

```bash
python3 -m series_projects.chapter_runner 14
```

实际项目代码在 `series_projects/`，公共基础设施在 `agent_examples/`。
