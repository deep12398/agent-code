# 13_chunking

本章不是孤立 demo，而是 `enterprise_rag_memory` 的第 13 章阶段入口。

连续项目：企业知识库 RAG + 长期记忆连续项目。

运行本章：

```bash
python3 13_chunking/demo.py
```

运行同一阶段的统一入口：

```bash
python3 -m series_projects.chapter_runner 13
```

实际项目代码在 `series_projects/`，公共基础设施在 `agent_examples/`。
